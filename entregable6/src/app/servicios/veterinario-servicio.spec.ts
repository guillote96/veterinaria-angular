import { VeterinarioServicio } from './veterinario-servicio';

describe('VeterinarioServicio', () => {
  it('should create an instance', () => {
    expect(new VeterinarioServicio()).toBeTruthy();
  });
});
